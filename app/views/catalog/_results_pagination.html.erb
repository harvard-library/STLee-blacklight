<% if show_pagination? and @response.total_pages > 1 %>
 <div class="row record-padding">
  <div class="col-md-12">
    <div class="pagination">
      <%= paginate @response, :window => 10, :left => 9, :right => 0, :outer_window => 0, :theme => 'blacklight' %>
    </div>
  </div>
 </div>
<% end %>
